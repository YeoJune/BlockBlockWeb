<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>피드백 작성</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>피드백 작성</h1>
        <nav>
          <a href="/feedback" class="btn">목록으로</a>
        </nav>
      </header>

      <main>
        <form class="feedback-form" action="/feedback" method="POST">
          <div class="form-group">
            <label for="author">작성자</label>
            <input type="text" id="author" name="author" required />
          </div>

          <div class="form-group">
            <label for="version">게임 버전</label>
            <select id="version" name="version_id" required>
              <% versions.forEach(version => { %>
              <option value="<%= version.id %>"><%= version.version %></option>
              <% }); %>
            </select>
          </div>

          <div class="form-group">
            <label for="content">내용</label>
            <textarea id="content" name="content" rows="10" required></textarea>
          </div>

          <button type="submit" class="btn submit-btn">작성하기</button>
        </form>
      </main>

      <footer>
        <small>&copy; 2025 Block Block. All rights reserved.</small>
      </footer>
    </div>
  </body>
</html>
