<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Block Block - 웹 버전</title>
    <link rel="stylesheet" href="/css/style.css" />
    <style>
      .game-container {
        width: 100%;
        height: calc(100vh - 100px);
        margin: 0 auto;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      #unity-container {
        width: 100%;
        height: 100%;
      }
      #unity-canvas {
        width: 100%;
        height: 100%;
        background: #000;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Block Block</h1>
        <nav>
          <a href="/" class="btn">메인으로</a>
          <a href="/versions" class="btn">모든 버전</a>
          <a href="/feedback" class="btn">피드백</a>
          <% if (isAdmin) { %>
          <a href="/admin" class="btn admin-btn">관리자</a>
          <a href="/admin/logout" class="btn logout-btn">로그아웃</a>
          <% } %>
        </nav>
      </header>

      <main>
        <div class="game-container">
          <div id="unity-container">
            <canvas id="unity-canvas"></canvas>
          </div>
        </div>
      </main>
    </div>

    <script src="/games/<%= version.filename %>/Build/UnityLoader.js"></script>
    <script>
      var unityInstance = UnityLoader.instantiate(
        "unity-container",
        "/games/<%= version.filename %>/Build/webgl.json"
      );
    </script>
  </body>
</html>
