<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>관리자 페이지</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>관리자 페이지</h1>
        <nav>
          <a href="/" class="btn">메인으로</a>
        </nav>
      </header>

      <main>
        <section class="upload-section">
          <h2>새 버전 업로드</h2>
          <form
            action="/admin/upload?password=<%= password %>"
            method="POST"
            enctype="multipart/form-data">
            <div class="form-group">
              <label for="version">버전</label>
              <input
                type="text"
                id="version"
                name="version"
                required
                placeholder="예: v0.1.0" />
            </div>

            <div class="form-group">
              <label for="game_file">게임 파일</label>
              <input type="file" id="game_file" name="game_file" required />
            </div>

            <button type="submit" class="btn">업로드</button>
          </form>
        </section>

        <section class="version-list">
          <h2>버전 목록</h2>
          <table class="version-table">
            <thead>
              <tr>
                <th>버전</th>
                <th>파일명</th>
                <th>업로드 날짜</th>
                <th>작업</th>
              </tr>
            </thead>
            <tbody>
              <% versions.forEach(version => { %>
              <tr>
                <td><%= version.version %></td>
                <td><%= version.filename %></td>
                <td><%= new Date(version.upload_date).toLocaleString() %></td>
                <td>
                  <form
                    action="/admin/delete?password=<%= password %>"
                    method="POST"
                    onsubmit="return confirm('정말 삭제하시겠습니까?');"
                    style="display: inline">
                    <input
                      type="hidden"
                      name="version_id"
                      value="<%= version.id %>" />
                    <input
                      type="hidden"
                      name="filename"
                      value="<%= version.filename %>" />
                    <button type="submit" class="btn btn-danger">삭제</button>
                  </form>
                </td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </section>
      </main>

      <footer>
        <small>&copy; 2025 Block Block. All rights reserved.</small>
      </footer>
    </div>
  </body>
</html>
